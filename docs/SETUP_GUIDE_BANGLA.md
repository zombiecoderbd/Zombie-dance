# ZombieCoder প্রজেক্ট সেটআপ গাইড 🧟‍♂️

এই গাইডে ZombieCoder প্রজেক্টের সম্পূর্ণ সেটআপ প্রক্রিয়া বর্ণনা করা হয়েছে। প্রতিটি ধাপ মনোযোগ সহকারে অনুসরণ করুন।

## 📋 পূর্বশর্ত (Prerequisites)

### প্রয়োজনীয় সফটওয়্যার:
- **Node.js** (v18 বা তার উপরে)
- **npm** বা **pnpm** (প্যাকেজ ম্যানেজার)
- **Git** (ভার্সন কন্ট্রোল)
- **VS Code** (এক্সটেনশন টেস্টিংয়ের জন্য)
- **Ollama** (লোকাল AI মডেলের জন্য)

### ঐচ্ছিক:
- **MySQL** (SQLite এর বিকল্প হিসেবে)
- **Docker** (কন্টেইনার ডেপ্লয়মেন্টের জন্য)

## 🚀 ধাপ ১: প্রজেক্ট ক্লোন ও ডিপেন্ডেন্সি ইনস্টল

### প্রজেক্ট ক্লোন করুন:
```bash
git clone https://github.com/zombiecoder1/zombiecoder.git
cd zombiecoder
```

### মূল প্রজেক্টের ডিপেন্ডেন্সি ইনস্টল:
```bash
npm install
# অথবা
pnpm install
```

### ব্যাকএন্ড ডিপেন্ডেন্সি ইনস্টল:
```bash
cd backend
npm install
cd ..
```

### এক্সটেনশন ডিপেন্ডেন্সি ইনস্টল:
```bash
cd extension
npm install
cd ..
```

## 🗄️ ধাপ ২: ডেটাবেজ সেটআপ

### অপশন A: SQLite (সহজ, সুপারিশকৃত)
```bash
cd temp
node setup_database.js
```

### অপশন B: MySQL
প্রথমে MySQL সার্ভার চালু করুন এবং ডেটাবেজ তৈরি করুন:
```sql
CREATE DATABASE zombi CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'zombi_user'@'localhost' IDENTIFIED BY '105585';
GRANT ALL PRIVILEGES ON zombi.* TO 'zombi_user'@'localhost';
FLUSH PRIVILEGES;
```

তারপর মাইগ্রেশন চালান:
```bash
cd temp
node setup_database.js --mysql
```

### ডেটাবেজ কনফিগারেশন যাচাই:
```bash
# SQLite ডেটাবেজ চেক
sqlite3 zombi.db "SELECT COUNT(*) FROM users;"

# MySQL ডেটাবেজ চেক
mysql -u root -p105585 zombi -e "SELECT COUNT(*) FROM users;"
```

## 🤖 ধাপ ৩: Ollama সেটআপ

### Ollama ইনস্টল (যদি ইনস্টল না থাকে):
```bash
# Linux/macOS
curl -fsSL https://ollama.ai/install.sh | sh

# Windows (PowerShell)
winget install Ollama.Ollama
```

### Ollama সার্ভিস চালু করুন:
```bash
ollama serve
```

### প্রয়োজনীয় মডেল ডাউনলোড:
```bash
# দ্রুত মডেল (সুপারিশকৃত)
ollama pull qwen2.5:0.5b

# উন্নত মডেল
ollama pull qwen2.5:1.5b

# এম্বেডিং মডেল (RAG এর জন্য)
ollama pull nomic-embed-text
```

### মডেল তালিকা চেক:
```bash
ollama list
```

## ⚙️ ধাপ ৪: Environment কনফিগারেশন

### .env ফাইল তৈরি/আপডেট:
```bash
# যদি setup_database.js চালিয়ে থাকেন, .env ফাইল ইতিমধ্যে তৈরি
# ম্যানুয়ালি এডিট করতে চাইলে:
cp .env.example .env
nano .env
```

### গুরুত্বপূর্ণ এনভায়রনমেন্ট ভেরিয়েবল:
```env
# ডেটাবেজ কনফিগারেশন
DB_TYPE=sqlite
DATABASE_URL=sqlite:./zombi.db

# সার্ভার কনফিগারেশন
PORT=8001
NODE_ENV=development

# Ollama কনফিগারেশন
OLLAMA_HOST=http://localhost:11434

# API কী (ঐচ্ছিক)
OPENAI_API_KEY=your_key_here
ANTHROPIC_API_KEY=your_key_here

# নিরাপত্তা
JWT_SECRET=your_generated_jwt_secret
```

## 🖥️ ধাপ ৫: সার্ভার চালু করা

### টার্মিনাল ১: ব্যাকএন্ড সার্ভার
```bash
cd backend
npm run dev
```

### টার্মিনাল ২: ফ্রন্টএন্ড সার্ভার
```bash
# মূল ডিরেক্টরিতে
npm run dev
```

### টার্মিনাল ৩: Ollama সার্ভিস (যদি আলাদাভাবে চালাতে হয়)
```bash
ollama serve
```

## 🧪 ধাপ ৬: টেস্টিং ও যাচাইকরণ

### ব্রাউজারে অ্যাক্সেস:
- **ফ্রন্টএন্ড**: http://localhost:3000
- **ব্যাকএন্ড API**: http://localhost:8001/v1/health
- **অ্যাডমিন প্যানেল**: http://localhost:3000/admin

### API এন্ডপয়েন্ট টেস্ট:
```bash
# স্বাস্থ্য পরীক্ষা
curl http://localhost:8001/v1/health

# রানটাইম স্ট্যাটাস
curl http://localhost:8001/v1/runtime_status

# এজেন্ট তথ্য
curl http://localhost:8001/v1/runtime_agent

# মডেল তালিকা
curl http://localhost:8001/api/models
```

### অ্যাডমিন প্যানেল লগইন:
- **ইউজারনেম**: admin
- **পাসওয়ার্ড**: admin123

## 🔧 ধাপ ৭: VS Code Extension সেটআপ

### Extension কম্পাইল:
```bash
cd extension
npm run compile
```

### Extension প্যাকেজিং:
```bash
# VSCE ইনস্টল (যদি না থাকে)
npm install -g @vscode/vsce

# Extension প্যাকেজ তৈরি
vsce package
```

### Extension ইনস্টল:
1. VS Code খুলুন
2. `Ctrl+Shift+P` চাপুন
3. "Extensions: Install from VSIX..." নির্বাচন করুন
4. তৈরি হওয়া .vsix ফাইল নির্বাচন করুন

### Extension টেস্ট:
1. `Ctrl+Shift+Z` চাপুন (ZombieCoder চ্যাট খোলার জন্য)
2. একটি টেস্ট প্রশ্ন লিখুন
3. Response আসছে কিনা দেখুন

## 🛠️ সমস্যা সমাধান (Troubleshooting)

### সাধারণ সমস্যা ও সমাধান:

#### ১. ব্যাকএন্ড সার্ভার চালু হচ্ছে না:
```bash
# পোর্ট ব্যস্ততা চেক
netstat -tulpn | grep 8001
# অন্য পোর্ট ব্যবহার করুন
PORT=8002 npm run dev
```

#### ২. ডেটাবেজ সংযোগ ত্রুটি:
```bash
# SQLite ফাইল পারমিশন চেক
ls -la zombi.db
chmod 664 zombi.db

# MySQL সার্ভিস চেক
sudo systemctl status mysql
```

#### ৩. Ollama মডেল লোড হচ্ছে না:
```bash
# Ollama সার্ভিস স্ট্যাটাস
curl http://localhost:11434/api/version

# মডেল পুনরায় ডাউনলোড
ollama pull qwen2.5:0.5b
```

#### ৪. Frontend বিল্ড ত্রুটি:
```bash
# Cache ক্লিয়ার
npm run clean
rm -rf .next node_modules
npm install
npm run build
```

#### ৫. Extension কাজ করছে না:
```bash
# TypeScript কম্পাইল চেক
cd extension
npx tsc --noEmit
npm run compile
```

## 📊 পারফরমেন্স অপটিমাইজেশন

### প্রোডাকশনের জন্য:
```bash
# ফ্রন্টএন্ড বিল্ড
npm run build

# ব্যাকএন্ড বিল্ড
cd backend
npm run build
npm start
```

### ডেটাবেজ অপটিমাইজেশন:
```sql
-- SQLite অপটিমাইজেশন
PRAGMA optimize;
PRAGMA wal_checkpoint(TRUNCATE);

-- MySQL অপটিমাইজেশন
OPTIMIZE TABLE users, chat_sessions, chat_messages;
```

## 🐳 Docker সেটআপ (ঐচ্ছিক)

### Docker Compose দিয়ে চালু:
```bash
# ডেভেলপমেন্ট মোড
docker-compose up -d

# প্রোডাকশন মোড
docker-compose -f docker-compose.prod.yml up -d
```

## 📝 লগ ও মনিটরিং

### লগ ফাইল চেক:
```bash
# ব্যাকএন্ড লগ
tail -f backend/logs/app.log

# সিস্টেম লগ
journalctl -u zombiecoder -f
```

### মনিটরিং URL:
- **সিস্টেম মেট্রিক্স**: http://localhost:8001/v1/runtime_status
- **অ্যাডমিন ড্যাশবোর্ড**: http://localhost:3000/admin
- **হেল্থ চেক**: http://localhost:8001/v1/health

## 🔐 নিরাপত্তা বিবেচনা

### প্রোডাকশনের জন্য:
1. **API কী সুরক্ষা**: পরিবেশ ভেরিয়েবলে রাখুন
2. **ডেটাবেজ ক্রেডেন্সিয়াল**: এনক্রিপ্ট করে রাখুন
3. **JWT সিক্রেট**: শক্তিশালী র্যান্ডম স্ট্রিং ব্যবহার করুন
4. **CORS**: নির্দিষ্ট ডোমেইনের জন্য কনফিগার করুন
5. **HTTPS**: প্রোডাকশনে SSL সার্টিফিকেট ব্যবহার করুন

## 📞 সাহায্য ও সাপোর্ট

### কমিউনিটি সাপোর্ট:
- **GitHub Issues**: https://github.com/zombiecoder1/zombiecoder/issues
- **ডিসকাশন**: https://github.com/zombiecoder1/zombiecoder/discussions

### ডকুমেন্টেশন:
- **API ডক্স**: `/docs/API.md`
- **আর্কিটেকচার**: `/docs/ARCHITECTURE.md`
- **কন্ট্রিবিউটিং**: `/CONTRIBUTING.md`

## 🎉 সেটআপ সম্পূর্ণ!

সফল সেটআপের পর আপনি:
- ✅ ZombieCoder চ্যাট ব্যবহার করতে পারবেন
- ✅ অ্যাডমিন প্যানেলে মডেল ম্যানেজ করতে পারবেন
- ✅ VS Code এ AI সহায়তা পাবেন
- ✅ কাস্টম মডেল যোগ করতে পারবেন

**সুখী কোডিং! 🧟‍♂️💻**

---

*এই গাইড নিয়মিত আপডেট হয়। সর্বশেষ ভার্সনের জন্য GitHub রেপোজিটরি চেক করুন।*
